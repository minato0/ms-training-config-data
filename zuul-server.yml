server:
  port: 8762
info:
  app:
    description: This is zuul server on spring boot application
    name: Zuul Server
    version: 1.0.0
zuul:
  ignored-services: '*'
  routes:
    auth-service:
      path: /auth/**
      service-id: auth-service
